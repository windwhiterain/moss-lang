a = 1;
diagnose {condition = a; source = @a; text = "should be 2";};

check = param -> {
    switch {index = equal [param, 2,]; set = [diagnose {condition = param; source = @param; text = "should be 2";},(),];};

};


b = check 1;
c = check 2;
